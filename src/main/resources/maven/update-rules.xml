<ruleset comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 https://www.mojohaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">
    <ignoreVersions>
        <ignoreVersion type="regex">(?i).*[-_\.]*(a|alpha|b|beta|rc|cr|m|ea)[-_\.]*[0-9]*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*alpha(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*a(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-alpha(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-beta(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*beta(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-B(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*-b(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*RC(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*CR(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i).*M(?:-?\d+)?</ignoreVersion>
        <ignoreVersion type="regex">(?i)\d\.\d\.\d-alpha(?:\d*)*</ignoreVersion>
        <ignoreVersion type="regex">(?i)\d\.\d\.\d-beta(?:\d*)*</ignoreVersion>
        <ignoreVersion type="regex">(?i).*CR(?:\d*)*</ignoreVersion>
        <ignoreVersion type="regex">(?i)(?:\d)*\.(?:\d)*\.(?:\d)*-CR(?:\d*)*</ignoreVersion>
    </ignoreVersions>
    <rules></rules>
</ruleset>